
# Prefer a reliable registry mirror; switch back to npmjs later if you want.
registry=https://registry.npmmirror.com/

# If corporate proxy MITMs SSL, uncomment the next line (NOT recommended on public Wi‑Fi).
# strict-ssl=false

# Make peer deps lenient for Node 22/npm 10+
legacy-peer-deps=true

# Longer timeouts for slow networks
fetch-timeout=600000
fetch-retries=3
